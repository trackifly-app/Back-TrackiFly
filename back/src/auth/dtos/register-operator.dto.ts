import { RegisterBaseDto } from './register-base.dto';

export class RegisterOperatorDto extends RegisterBaseDto {
  // El operador por ahora hereda los campos base del usuario y no tiene perfil extendido
}
